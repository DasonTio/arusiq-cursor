# src/routes

Owner: **platform · every route declares allowedRoles**

See `context/40-architecture.md` for the full ownership table and the
dependency direction (features → patterns → components → design-system).
